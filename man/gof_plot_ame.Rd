% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gof_dynamic_ame.R
\name{gof_plot_ame}
\alias{gof_plot_ame}
\title{Plot a goodness-of-fit result}
\usage{
gof_plot_ame(
  gof,
  what = c("replication", "latent", "decomposition", "residual"),
  statistics = NULL,
  labels = NULL,
  style = NULL,
  ...
)
}
\arguments{
\item{gof}{A \code{gof_dynamic_ame} object.}

\item{what}{Which figure: \code{"replication"}, \code{"latent"}, \code{"decomposition"} or
\code{"residual"}.}

\item{statistics}{For \code{what = "replication"}, which statistics to draw.
\code{NULL} draws every one except \code{sd.delta.cell}, which is an exact function
of \code{sd.cell} at both periods and the \code{cor.lag1} between them and so adds a
panel without adding information. Name it explicitly to see it — in the
outcome's own units it is the more legible of the two, so it is worth a
look when the temporal panels are the point.}

\item{labels}{Panel titles, as a named character vector keyed by statistic,
e.g. \code{c(sd.rowmean = "Country heterogeneity")}. Partial: anything not
named keeps its statistic's own name. Panels are titled with the raw
statistic name by default, so that the figure, \code{gof$replication} and the
documentation all use one vocabulary; rename them here when the figure has
to stand on its own, where "row node" and "column node" have names of
their own. Any note the panel carries is appended after the new title, the
note being about the statistic rather than about what it is called.}

\item{style}{A \code{\link[=gof_style]{gof_style()}} object setting line widths, point size and
shape, colours, line types and the reference band. \code{NULL} draws the
default look.}

\item{...}{Ignored.}
}
\value{
A \code{ggplot} object. Type sizes and everything else are set
afterwards the usual way: \code{+ ggtitle()}, \code{+ labs()}, \code{+ theme()}. Panel
titles are baked into the data, which is why \code{labels} exists; drawing
parameters are baked into the layers, which is why \code{style} exists.
}
\description{
Every panel shows the observed statistic as a solid line with filled points
against a shaded reference band and its dashed median. Colour and line type
both carry the distinction, so the figure survives greyscale printing and
colour vision deficiency.
}
