% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_temporal_bipartite_als.R
\name{bootstrap_temporal_bipartite_als}
\alias{bootstrap_temporal_bipartite_als}
\title{Bootstrap inference for a fitted temporal bipartite ALS panel (deprecated)}
\usage{
bootstrap_temporal_bipartite_als(
  fit,
  edge_panel,
  row_cov_df = NULL,
  col_cov_df = NULL,
  dyad_cov_df = NULL,
  row_covar_names = character(0),
  col_covar_names = character(0),
  dyad_covar_names = character(0),
  time_col = "year",
  row_col = "node_row",
  col_col = "node_col",
  value_col = "value",
  row_prefix = "row_",
  col_prefix = "col_",
  row_pad = NULL,
  col_pad = NULL,
  transform = identity,
  row_cov_time_col = time_col,
  row_cov_id_col = row_col,
  row_cov_prefix = row_prefix,
  col_cov_time_col = time_col,
  col_cov_id_col = col_col,
  col_cov_prefix = col_prefix,
  dyad_cov_time_col = time_col,
  dyad_cov_row_id_col = row_col,
  dyad_cov_col_id_col = col_col,
  dyad_cov_row_prefix = row_prefix,
  dyad_cov_col_prefix = col_prefix,
  demean_row_covariates = FALSE,
  demean_col_covariates = FALSE,
  K = 2,
  lambda = 0.1,
  gamma = 1,
  max_iter = 1000,
  tol = 1e-05,
  B = 200,
  bootstrap_type = c("wild", "residual", "parametric"),
  include_alpha = TRUE,
  include_beta = TRUE,
  use_original_as_init = TRUE,
  conf_level = 0.95,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{fit}{A fitted object returned by \code{fit_temporal_bipartite_als}.}

\item{edge_panel}{Main edge-panel data frame (same one used to produce \code{fit}).}

\item{row_cov_df}{Optional row covariate data frame.}

\item{col_cov_df}{Optional column covariate data frame.}

\item{dyad_cov_df}{Optional dyadic covariate data frame.}

\item{row_covar_names}{Character vector of row covariate names.}

\item{col_covar_names}{Character vector of column covariate names.}

\item{dyad_covar_names}{Character vector of dyadic covariate names.}

\item{time_col}{Name of the time column in \code{edge_panel}.}

\item{row_col}{Name of the row-node column in \code{edge_panel}.}

\item{col_col}{Name of the column-node column in \code{edge_panel}.}

\item{value_col}{Name of the value column in \code{edge_panel}.}

\item{row_prefix}{Prefix applied to row-node IDs.}

\item{col_prefix}{Prefix applied to column-node IDs.}

\item{row_pad}{Optional integer width for left-padding row IDs.}

\item{col_pad}{Optional integer width for left-padding column IDs.}

\item{transform}{Transformation applied to matrix entries.}

\item{row_cov_time_col}{Time column in \code{row_cov_df}.}

\item{row_cov_id_col}{Row-node ID column in \code{row_cov_df}.}

\item{row_cov_prefix}{Prefix applied to \code{row_cov_id_col}.}

\item{col_cov_time_col}{Time column in \code{col_cov_df}.}

\item{col_cov_id_col}{Column-node ID column in \code{col_cov_df}.}

\item{col_cov_prefix}{Prefix applied to \code{col_cov_id_col}.}

\item{dyad_cov_time_col}{Time column in \code{dyad_cov_df}.}

\item{dyad_cov_row_id_col}{Row-node ID column in \code{dyad_cov_df}.}

\item{dyad_cov_col_id_col}{Column-node ID column in \code{dyad_cov_df}.}

\item{dyad_cov_row_prefix}{Prefix applied to dyadic row IDs.}

\item{dyad_cov_col_prefix}{Prefix applied to dyadic column IDs.}

\item{demean_row_covariates}{Logical. Apply within-node time demeaning to
row covariates before bootstrap (should match the original \code{fit}).}

\item{demean_col_covariates}{Logical. Same for column covariates.}

\item{K}{Latent dimension (must match \code{fit}).}

\item{lambda}{Ridge penalty (must match \code{fit}).}

\item{gamma}{Temporal smoothing penalty (must match \code{fit}).}

\item{max_iter}{Maximum ALS iterations per bootstrap replication.}

\item{tol}{Convergence tolerance.}

\item{B}{Number of bootstrap replications per period.}

\item{bootstrap_type}{One of \code{"residual"}, \code{"wild"}, or
\code{"parametric"}.  \code{"wild"} is recommended for heteroskedastic
trade data.}

\item{include_alpha}{Logical; include row fixed effects in coefficient
summaries.}

\item{include_beta}{Logical; include column fixed effects in coefficient
summaries.}

\item{use_original_as_init}{Logical; if \code{TRUE} each bootstrap
replication is initialised at the original period estimate (faster
convergence, recommended).}

\item{conf_level}{Confidence level for bootstrap intervals.}

\item{seed}{Optional integer random seed.  Period \emph{tt} uses
\code{seed + tt - 1} so results are reproducible yet vary across periods.}

\item{verbose}{Logical; print progress messages.}
}
\value{
An object of class \code{"bootstrap_temporal_bipartite_als"}, a list
with components:
\describe{
\item{\code{period_results}}{Named list (one entry per period) of
\code{bootstrap_als_factorize_joint_cov} objects.}
\item{\code{coef_df}}{Long data frame with columns
\code{period}, \code{term}, \code{estimate}, \code{std.error},
\code{conf.low}, \code{conf.high} — stacked across all periods.}
\item{\code{latent_df}}{Long data frame with columns
\code{period}, \code{row_id}, \code{col_id}, \code{mean}, \code{sd},
\code{conf.low}, \code{conf.high}, \code{sign.prob} — stacked across
all periods.}
\item{\code{success_df}}{Data frame reporting \code{B_requested} and
\code{B_successful} for each period.}
\item{\code{params}}{List of all hyperparameters used.}
}
}
\description{
\strong{Deprecated.} Operates on the per-period fit objects produced by the
deprecated \code{\link[=fit_temporal_bipartite_als]{fit_temporal_bipartite_als()}}. A joint-trajectory bootstrap for
\code{\link[=fit_dynamic_ame]{fit_dynamic_ame()}} is not implemented yet.
}
\seealso{
\code{\link[=fit_dynamic_ame]{fit_dynamic_ame()}}

Runs \code{bootstrap_als_factorize_joint_cov} for every period in the
panel, automatically threading the previous-period anchors
(\code{U_prev}, \code{V_prev}, \code{alpha_prev}, \code{beta_prev},
\code{coef_row_prev}, \code{coef_col_prev}, \code{coef_dyad_prev})
across time.  All arguments mirror \code{fit_temporal_bipartite_als}
exactly so you can copy-paste your fitting call and just add the
bootstrap-specific parameters.
}
