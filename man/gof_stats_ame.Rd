% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gof_dynamic_ame.R
\name{gof_stats_ame}
\alias{gof_stats_ame}
\title{Goodness-of-fit statistics for a panel of networks}
\usage{
gof_stats_ame(
  Y_list,
  min_overlap = 5L,
  which = c("sd.cell", "sd.rowmean", "sd.colmean", "cor.lag1", "sd.delta.cell",
    "sd.rowcor", "sd.colcor"),
  periods = NULL
)
}
\arguments{
\item{Y_list}{List of \code{T} outcome matrices, \code{NA} where unobserved.}

\item{min_overlap}{Minimum jointly observed cells for a pair of rows or
columns to contribute to \code{sd.rowcor} or \code{sd.colcor}.}

\item{which}{Statistics to compute. Defaults to all seven; the two
correlation-spread statistics are much the most expensive, so the
replication group asks only for the ones it uses.}

\item{periods}{Optional labels, one per element of \code{Y_list}.}
}
\value{
A data frame with \code{statistic}, \code{period}, \code{value} and \code{n_used}.
}
\description{
Computes the seven statistics \code{\link[=gof_dynamic_ame]{gof_dynamic_ame()}} compares, directly from a
panel. No fitted model is involved, so the same numbers can be taken from a
simulated panel, a competing model's replicates, or the data itself.
}
\details{
Five are per period:

\describe{
\item{\code{sd.cell}}{Spread of the cells. A scale reference.}
\item{\code{sd.rowmean}}{Spread of the row means: how unequal the row nodes are.}
\item{\code{sd.colmean}}{Spread of the column means: how unequal the column
nodes are.}
\item{\code{sd.rowcor}}{Spread of the correlations between row profiles. Large
when rows differ in the \emph{shape} of their profile across columns, which no
additive structure produces; see \code{.stat_profile_cor()}.}
\item{\code{sd.colcor}}{The same along the other margin.}
}

Two describe a transition, and are labelled by the period they arrive at, so
they plot on the same axis as the rest:

\describe{
\item{\code{cor.lag1}}{Correlation between consecutive panels: how similar
adjacent periods are.}
\item{\code{sd.delta.cell}}{Spread of the cell-by-cell changes: how much the
panel moves, in the units of the outcome.}
}

Both transition statistics use only cells observed in both periods. On a
common cell set the two are related through \code{sd.cell}, by
\verb{var(Y_t - Y_\{t-1\}) = var(Y_t) + var(Y_\{t-1\}) - 2 cov(Y_t, Y_\{t-1\})}, so
\code{sd.delta.cell} carries no information the other three lack. It is reported
because a movement in outcome units is legible where a correlation is not.
}
\examples{
\dontrun{
gof_stats_ame(fit$Y_list)
}
}
