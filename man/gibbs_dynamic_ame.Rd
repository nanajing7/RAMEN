% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gibbs_dynamic_ame.R
\name{gibbs_dynamic_ame}
\alias{gibbs_dynamic_ame}
\title{Gibbs sampler for a dynamic bipartite AME model}
\usage{
gibbs_dynamic_ame(
  Y_list,
  X_row_list = NULL,
  X_col_list = NULL,
  X_dyad_list = NULL,
  K = 2,
  n_iter = 5000,
  burn = 1000,
  thin = 1,
  store_latent = FALSE,
  prior = list(a0 = 0.001, b0 = 0.001),
  latent_innovation = c("shared", "separate"),
  init = NULL,
  covariate_names = NULL,
  years = NULL,
  seed = 1,
  verbose = FALSE
)
}
\arguments{
\item{Y_list}{List of \code{T} outcome matrices, \code{NA} where unobserved.}

\item{X_row_list, X_col_list, X_dyad_list}{Covariate lists, or \code{NULL}.}

\item{K}{Latent dimension.}

\item{n_iter}{Sweeps to keep after \code{burn}.}

\item{burn}{Sweeps to discard.}

\item{thin}{Keep every \code{thin}-th sweep.}

\item{store_latent}{Also keep \verb{U_t V_t'} for every kept sweep. Memory grows
as \code{n_iter/thin * T * N * M}; off by default.}

\item{prior}{Inverse-gamma shape \code{a0} and rate \code{b0} for every variance.}

\item{latent_innovation}{Whether the latent block has one innovation
variance or two, matching the argument of the same name in
\code{\link[=fit_dynamic_ame]{fit_dynamic_ame()}}. Under \code{"shared"} a single \code{kappa} is drawn from the
pooled sufficient statistics of both sides — the full conditional the model
implies, not an average of two draws. The default matches the estimator's,
because a comparison between the two inference paths is a comparison of
nothing unless both are fitting the same model. \code{"separate"} draws them
independently, as earlier versions did.}

\item{init}{Optional starting parameters, as returned in \verb{$params}. Chains
of a diagnostic run must start far apart or their agreement carries no
information; when \code{NULL} the start is drawn from a diffuse Gaussian.}

\item{covariate_names}{Row names for \code{beta}; taken from \code{X_dyad_list} when
available.}

\item{years}{Period labels used to name the stored columns.}

\item{seed}{Random seed.}

\item{verbose}{Report progress every 500 sweeps.}
}
\value{
An object of class \code{gibbs_dynamic_ame}: \code{beta} (kept sweeps by
\code{P * T}, columns named \code{covariate:period}), \code{omega} (kept sweeps by 11),
\code{latent} when requested, \code{params} (last state, for restarting), \code{settings},
and \code{seconds}.
}
\description{
Samples the posterior of the model \code{\link[=fit_dynamic_ame]{fit_dynamic_ame()}} maximises. Provided
for comparing the cost of the two inference paths on equal terms, and for
users who want full posterior inference; the package's primary estimator
remains \code{\link[=fit_dynamic_ame]{fit_dynamic_ame()}}.
}
\details{
Every conditional is closed form, so there is nothing to tune and no
acceptance rate to watch. A sweep solves the same block-tridiagonal systems
the optimiser solves, drawing from each rather than taking its mean, and so
costs about one inner iteration of the block coordinate descent.

The data arrive already prepared. Building them from an edge panel is shared
work belonging to neither inference path, and charging it to one of them
would measure the wrong thing; \code{\link[=gibbs_from_fit]{gibbs_from_fit()}} takes them from a fitted
object.

\verb{U_t V_t'} is unchanged by \verb{(U_t A, V_t A^-T)} for constant invertible \code{A}.
The reciprocal-scale half of that freedom is fixed after every sweep, by the
same normalisation the estimator applies: left alone one factor grows while
the other shrinks with the product exactly fixed, and \code{sigma_U^2} and
\code{tau_U^2} end up describing where the chain drifted rather than the data.
The rotation is left alone, the variance components being invariant to it.
The columns of \code{U} and \code{V} therefore still wander and their convergence
diagnostics never settle, which is a property of the parameter rather than
of the sampler; monitor \code{beta}, the variance components, or the products
\verb{U_t V_t'}, all of which are identified.
}
\examples{
\dontrun{
fit <- fit_dynamic_ame(edge_panel = my_panel, K = 2)
g <- gibbs_from_fit(fit, n_iter = 5000, burn = 1000)
summary(g)
}
}
