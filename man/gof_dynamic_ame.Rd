% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gof_dynamic_ame.R
\name{gof_dynamic_ame}
\alias{gof_dynamic_ame}
\title{Goodness of fit for a dynamic bipartite AME model}
\usage{
gof_dynamic_ame(
  fit,
  nsim = 500,
  conf_level = 0.95,
  min_overlap = 5L,
  keep_draws = FALSE,
  seed = 1,
  n_cores = 1,
  verbose = FALSE
)
}
\arguments{
\item{fit}{A \code{dynamic_ame} object from \code{\link[=fit_dynamic_ame]{fit_dynamic_ame()}}.}

\item{nsim}{Simulated panels per reference distribution. \code{0} skips both
simulation groups, leaving the accounting and diagnostics, which need no
simulation.}

\item{conf_level}{Width of the reference intervals.}

\item{min_overlap}{Minimum jointly observed cells for a pair of rows or
columns to contribute to \code{sd.rowcor} or \code{sd.colcor}.}

\item{keep_draws}{Retain every replicate's statistics, which allows a
different \code{conf_level} to be applied afterwards without simulating again.}

\item{seed}{Base seed. The two simulation groups are seeded independently of
\code{\link[=bootstrap_ame]{bootstrap_ame()}}; nothing here is shared with a bootstrap run.}

\item{n_cores}{Workers for the simulation groups. Both are cheap on panels of
moderate size; the cost that grows is \code{sd.rowcor}, at order
\code{T * N^2 * M} per replicate.}

\item{verbose}{Report progress.}
}
\value{
An object of class \code{gof_dynamic_ame}: a list of tidy data frames,
\code{replication}, \code{latent_reference}, \code{increment}, \code{decomposition},
\code{estimator}, \code{residual}, \code{skipped} and \code{settings}.
}
\description{
Describes one fit: whether it reproduces the panel it was estimated from,
what each block explains, whether the estimator finished cleanly, and whether
structure is left in the residuals. Nothing here refits a model, and nothing
here is a hypothesis test.
}
\section{What the groups answer}{


\describe{
\item{\verb{$replication}}{Simulates panels that hold the fitted structure and
redraw the observation errors, and compares five panel statistics against
the resulting spread. See the caveat below: these are reference
intervals, not predictive intervals.}
\item{\verb{$latent_reference}}{Compares the spread of row- and column-profile
correlations against panels rebuilt from the covariate and additive terms
with the pair-level variation replaced by noise of the same mean and
variance. A large positive \code{excess} says the data carry cross-dyad
structure that an additive fit plus independent noise does not reproduce.}
\item{\verb{$decomposition}, \verb{$increment}}{In-sample accounting.}
\item{\verb{$estimator}}{Convergence, and whether any limit was binding.}
\item{\verb{$residual}}{Row, column, temporal and distributional screening of
the residuals.}
}
}

\section{Reading the replication intervals}{


They are descriptive conditional-reference intervals and \strong{do not have
nominal coverage}. An observed value inside the interval is compatible with
the fitted conditional structure; a value outside indicates a discrepancy
worth inspecting. Every replicate holds \code{U} and \code{V} at their estimates and
gives \code{beta}, \code{a} and \code{b} only the limited dispersion adjustment the package
supplies, so the interval is narrow by an unquantified amount; its centre is
also built from the data the observed value comes from. \code{inside} is therefore
not a pass mark and never appears among the warnings, and \code{tail_fraction} is
the fraction of replicates at least as far from the reference median as the
observation, not a p-value.

Two statistics are marked \code{by_construction} and are not independent evidence.
\code{sd.cell} is nearly matched by construction, the replicate variance being
\code{var(fitted) + sigma_eps^2} while \code{sigma_eps^2} is the residual mean square,
so the leading terms cancel. \code{sd.delta.cell} is an exact function of \code{sd.cell}
at both periods and the \code{cor.lag1} between them. Both are reported because
scale and movement in the outcome's own units are legible in a way the
remaining statistics are not.
}

\section{What this does not do}{


It is not a posterior predictive check. It does not test whether the
random-walk transition is correct: \code{cor.lag1} measures adjacent-panel
similarity, which stable covariates and a latent structure held fixed across
replicates both raise. It does not establish that the multiplicative term is
worth estimating, and \verb{$latent_reference} is not a latent-factor significance
test, since unmodelled dyadic dependence, heteroskedasticity, or a
misspecified covariate can also produce excess. Those questions need a second
model fitted to the same data and belong to model comparison. Zero mass,
degree distributions and four-cycle counts are not checked: a Gaussian
outcome model does not claim to reproduce them.
}

\examples{
\dontrun{
fit <- fit_dynamic_ame(edge_panel = my_panel, K = 2)
g <- gof_dynamic_ame(fit, nsim = 500)
g
gof_plot_ame(g, "replication")
}
}
\seealso{
\code{\link[=gof_stats_ame]{gof_stats_ame()}} for the statistics on their own,
\code{\link[=gof_plot_ame]{gof_plot_ame()}} to plot the result.
}
