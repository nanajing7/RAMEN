% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gof_dynamic_ame.R
\name{gof_style}
\alias{gof_style}
\title{Appearance of goodness-of-fit figures}
\usage{
gof_style(
  linewidth = NULL,
  ref_linewidth = NULL,
  point_size = NULL,
  point_shape = NULL,
  colours = NULL,
  linetypes = NULL,
  band_fill = NULL,
  band_alpha = NULL
)
}
\arguments{
\item{linewidth}{Width of the observed line, and of the block lines in the
decomposition figure.}

\item{ref_linewidth}{Width of the reference line.}

\item{point_size}{Size of the plotted points.}

\item{point_shape}{Shape of the plotted points, as a ggplot2 shape code.}

\item{colours}{Named character vector of colours, keyed by role.}

\item{linetypes}{Named vector of line types, keyed by role. Numeric codes
are accepted and translated to their names.}

\item{band_fill}{Fill of the reference band. In the latent figure it
follows the \code{reference} colour unless set here.}

\item{band_alpha}{Opacity of the reference band, between 0 and 1.}
}
\value{
An object of class \code{gof_style}.
}
\description{
Collects the drawing parameters that \code{\link[=gof_plot_ame]{gof_plot_ame()}} would otherwise fix
inside its layers -- line widths, point size and shape, colours, line types
and the reference band -- so they can be set from outside, the way type
sizes are set through \code{theme()}. Font sizes are deliberately not here:
\code{+ ggplot2::theme()} already does that, and a second route to the same
setting would only let the two disagree.
}
\details{
Every argument defaults to \code{NULL}, meaning "use the figure's own default",
so \code{gof_style()} with no arguments reproduces the default look exactly and
only what is named changes. Defaults differ between figures on purpose --
the replication reference is neutral grey because it is this model, the
latent reference is blue because it is a different one -- which is why a
\code{NULL} here defers to the figure rather than fixing one value for all.

Colours and line types are named by ROLE, not by legend text, so renaming
panels or legend entries never disconnects a colour from its series:

\describe{
\item{\code{observed}}{the observed statistic (replication, latent), and the
points of the residual quantile plot}
\item{\code{reference}}{the reference median line (replication, latent), and
the 45-degree line of the residual quantile plot}
\item{\code{covariate}, \code{additive}, \code{latent}, \code{residual}}{the four blocks of
the decomposition figure}
}

Roles a figure does not draw are ignored by it, so one style object can be
passed to all four figures.
}
\examples{
\dontrun{
st <- gof_style(linewidth = 1.4, point_size = 3,
                colours = c(observed = "black"),
                linetypes = c(reference = "dotted"))
gof_plot_ame(gof, "replication", style = st) +
  ggplot2::theme(strip.text = ggplot2::element_text(size = 18))
}
}
